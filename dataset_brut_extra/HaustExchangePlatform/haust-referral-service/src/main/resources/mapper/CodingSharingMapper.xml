<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.haust.referral.mapper.CodingSharingMapper">


    <insert id="insert">
        insert into coding_sharing (user_id, company_name, detail, remark,
                                    recommander_email, recommand_index, code_id)
        values (
                   #{userId},#{companyName},#{detail},#{remark},#{recommanderEmail},#{recommandIndex},#{codeId}
               )
    </insert>
    <update id="update">
        update coding_sharing
        <set>
            <if test="userId != null">
                user_id = #{userId},
            </if>
            <if test="companyName != null">
                company_name = #{companyName},
            </if>
            <if test="detail != null">
                detail = #{detail},
            </if>
            <if test="remark != null">
                remark = #{remark},
            </if>
            <if test="recommanderEmail != null">
                recommander_email = #{recommanderEmail},
            </if>
            <if test="clickNumber != null">
                click_number = #{clickNumber},
            </if>
            <if test="recommandIndex != null">
                recommand_index = #{recommandIndex},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="codeId != null">
                code_id = #{codeId},
            </if>
        </set>
        where user_id = #{userId} and id = #{id}
    </update>
    <update id="permitInfo">
        update coding_sharing
        set status =#{status}
        <where>
            <if test="id!=null">
                AND id = #{id}
            </if>
        </where>
    </update>
    <delete id="delete" parameterType="java.lang.Long">
        delete from coding_sharing
        <where>
            <if test="id!=null">
                id = #{id}
            </if>
        </where>
    </delete>

    <select id="getDetailById" resultType="com.haust.common.domain.po.CodingSharing" parameterType="java.lang.Long" >
        select id,user_id, company_name, detail, remark, recommander_email, click_number, recommand_index, status, code_id, create_time, update_time  from coding_sharing where id = #{id}
    </select>



    <select id="page" resultType="com.haust.common.domain.vo.CodingSharingVo"
            parameterType="com.haust.common.domain.dto.PageDTO">
        select
        id,
        company_name as companyName,
        code_id as codeId,
        click_number as clickNumber,
        create_time as createDate  from coding_sharing
        <where>
            <if test="status!=null">
                and status = #{status}
            </if>
        </where>
        order by
            <choose>
                <when test="orderBy == 1">
                    click_number DESC
                </when>
                <otherwise>
                    create_time DESC
                </otherwise>
            </choose>
    </select>
    <select id="pageMyInfo" resultType="com.haust.common.domain.po.CodingSharing" parameterType="com.haust.common.domain.dto.PageDTO">
        select
        id,
        user_id as userId,
        recommand_index as recommandIndex,
        recommander_email as recommanderEmail,
        remark,
        status,
        detail,
        company_name as companyName,
        code_id as codeId,
        click_number as clickNumber,
        create_time as createDate,
        update_time as updateTime
        from coding_sharing
        <where>
            <if test="userId!=null">
                user_id=#{userId}
            </if>
        </where>
        order by
        <choose>
            <when test="pageDTO.orderBy == 1">
                click_number DESC
            </when>
            <otherwise>
                create_time DESC
            </otherwise>
        </choose>
    </select>

</mapper>
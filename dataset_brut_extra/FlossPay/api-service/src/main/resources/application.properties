# =============================================================================
# Logging Configuration — OpenPay API Service
# =============================================================================

# Custom log pattern: log level (right-padded), include requestId from MDC
logging.pattern.level=%5p [reqId=%X{requestId}]

# Set default log level (INFO for production)
logging.level.root=INFO

# Uncomment to enable debug logging for Spring framework
# logging.level.org.springframework=DEBUG

# Uncomment to enable debug logging for your own code
# logging.level.com.openpay=DEBUG

# =============================================================================
# Notes:
# - Ensure your logback.xml/log4j2.xml is configured to use MDC (requestId).
# - Use DEBUG levels only in development/testing.
# =============================================================================

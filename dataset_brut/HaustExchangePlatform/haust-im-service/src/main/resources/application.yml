server:
  port: 8084
spring:
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  application:
    name: haust-im-service
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848
  redis:
    port: ${haust.redis.port:6379}
    host: ${haust.redis.host:localhost}
    connect-timeout: ${haust.redis.timeout:200ms}
  rabbitmq:
    port: ${haust.rabbitmq.port:5672}
    host: ${haust.rabbitmq.host:117.72.112.62}
    password: ${haust.rabbitmq.password:200522}
    virtual-host: ${haust.rabbitmq.virtual-host:/}
    username: ${haust.rabbitmq.username:root}

springfox:
  documentation:
    enabled: false
